import { ItemTypes } from "../../types/items";

const appsItems = {
  whatsapp: {
    id: "whatsapp",
    displayName: "whats app",
    open: false,
    type: ItemTypes.app,
  },
};

export default appsItems;
